{% extends 'base.html.twig' %}

{% block title %}ObjetCarnet{% endblock %}

{% block body %}
    <h1>ObjetCarnet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objet_carnet.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ objet_carnet.type }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ objet_carnet.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('objet_carnet_index') }}">back to list</a>

    <a href="{{ path('objet_carnet_edit', {'id': objet_carnet.id}) }}">edit</a>

    {{ include('objet_carnet/_delete_form.html.twig') }}
{% endblock %}

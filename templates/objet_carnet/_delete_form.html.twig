<form method="post" action="{{ path('objet_carnet_delete', {'id': objet_carnet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objet_carnet.id) }}">
    <button class="btn">Delete</button>
</form>

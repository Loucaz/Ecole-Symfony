{% extends 'base.html.twig' %}

{% block body %}
    {% include 'menu.html.twig' %}
    <div class="container">
        <br>
        <h1>Créer une nouvelle classe</h1>

        {{ include('classe/_form.html.twig') }}

        <br>
        <a href="{{ path('classe_index') }}" class="btn btn-primary">Retour à la liste</a>
    </div>

{% endblock %}

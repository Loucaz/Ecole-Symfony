<form method="post" action="{{ path('photo_de_classe_delete', {'id': photo_de_classe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photo_de_classe.id) }}">
    <button class="btn">Delete</button>
</form>

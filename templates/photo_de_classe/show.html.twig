{% extends 'base.html.twig' %}

{% block title %}PhotoDeClasse{% endblock %}

{% block body %}
    <h1>PhotoDeClasse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo_de_classe.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ photo_de_classe.annee ? photo_de_classe.annee|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ photo_de_classe.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('photo_de_classe_index') }}">back to list</a>

    <a href="{{ path('photo_de_classe_edit', {'id': photo_de_classe.id}) }}">edit</a>

    {{ include('photo_de_classe/_delete_form.html.twig') }}
{% endblock %}
